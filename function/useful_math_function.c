#include <stdio.h>
#include <math.h>
int main()
{
    int ans = abs(-10);
    // int ans = pow(4, 3);
    // int ans = round(4.3);
    // int ans = sqrt(49);
    // int ans = floor(5.2);
    // int ans = ceil(5.2);
    printf("%d", ans);
    return 0;
}